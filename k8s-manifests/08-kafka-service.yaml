apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: eda-app
spec:
  selector:
    app: kafka # Doit correspondre aux labels du Pod Kafka
  ports:
    - protocol: TCP
      port: 9092
      targetPort: 9092
  type: ClusterIP 